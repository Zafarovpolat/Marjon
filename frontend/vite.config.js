import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      "/api": {
        target: "http://127.0.0.1:8000",
        changeOrigin: true,
      },
    },
  },
  build: {
    // ВНИМАНИЕ: cssTarget здесь сознательно НЕ задан.
    //
    // Каскад проекта держится на @layer, который появился весной 2022
    // (Chrome 99, Firefox 97, Safari 15.4). Возникает соблазн отразить это в
    // cssTarget — я так и сделал, и это оказалось ошибкой сразу дважды.
    //
    // Во-первых, это ничего не чинит: сборщик не умеет разворачивать @layer в
    // старый синтаксис, и в Safari 14 слои всё равно не заработают. Настройка
    // лишь убирает предупреждение.
    //
    // Во-вторых, она молча меняет обработку ВСЕГО остального CSS: набор
    // автопрефиксов и то, какие современные конструкции разворачиваются в
    // старые. На графике админки это дало холст на 7px выше — сравнение
    // картинок поймало, а сравнение самих стилей показало ноль расхождений,
    // потому что CSS-то не менялся, менялась сборка.
    //
    // Требование к браузеру — вопрос среды выполнения (приложение живёт в
    // Electron и в актуальных браузерах), а не настройки сборщика.
    rollupOptions: {
      input: {
        kafe: "index.html",
        admin: "admin.html",
      },
    },
  },
  test: {
    environment: "jsdom",
    setupFiles: "./src/test/setup.js",
    include: ["src/**/*.test.{js,jsx}"],
    clearMocks: true,
    restoreMocks: true,
  },
});
